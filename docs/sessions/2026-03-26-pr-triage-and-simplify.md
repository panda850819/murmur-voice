---
date: 2026-03-26
branch: main
project: murmur-voice
tags: [coding-session, murmur-voice, pr-triage, code-review]
---

# pr-triage-and-simplify — 2026-03-26

## What happened

Reviewed all 31 open PRs in the repo, all auto-generated by Bolt (performance) and Sentinel (security) bots. Found extreme duplication — 31 PRs covered only 7 unique issues. Merged the 5 best candidates (#69 open_url scheme validation, #92 API key password input, #90 debug log cleanup, #74 Intl.Segmenter cache, #91 resample loop split) and closed the remaining 25 duplicates. Then ran /simplify on the merged code, which caught a correctness bug in wordDiff (pre-lowercasing broke locale-sensitive segmentation), improved the URL allowlist to use a const array, and cleaned up redundant comments. All 48 Rust tests pass.

## Retrospective

- The Bolt/Sentinel bots have no deduplication — they keep submitting the same fix with minor cosmetic differences. This needs a config fix on the bot side.
- Bot-generated severity labels are consistently inflated (everything marked CRITICAL/HIGH). Should not trust these at face value.
- The wordDiff "optimization" (lowercase before tokenize) was a subtle correctness regression that passed review — reinforces the value of running /simplify after merging bot PRs.

## Current state

All changes committed and pushed to main. v0.4.3 release unchanged (these are post-release cleanups). Build cache was cleaned due to stale path reference (site/projects vs site/personal/apps).

## Follow-ups

- [ ] Fix Bolt/Sentinel bot config to track existing PRs and avoid duplicate submissions
- [ ] Consider disabling or reducing bot PR frequency — most optimizations are micro-level with negligible real-world impact
